<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:id="@+id/detailRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:padding="16dp">

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        card_view:cardCornerRadius="12dp"
        card_view:cardElevation="4dp"
        android:layout_marginBottom="16dp"
        android:layout_marginTop="8dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Top Bar -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingBottom="8dp">

                <ImageView
                    android:id="@+id/backButton"
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:src="@drawable/ic_back" />

                <TextView
                    android:id="@+id/titleText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Protein Name"
                    android:textSize="22sp"
                    android:textStyle="bold"
                    android:textColor="#000000"
                    android:layout_marginStart="16dp" />
            </LinearLayout>

            <!-- Info Fields -->
            <TextView android:id="@+id/sequenceText"
                style="@style/InfoLabel" />
            <TextView android:id="@+id/functionText"
                style="@style/InfoLabel" />
            <TextView android:id="@+id/sourceText"
                style="@style/InfoLabel" />
            <TextView android:id="@+id/weightText"
                style="@style/InfoLabel" />
            <TextView android:id="@+id/typeText"
                style="@style/InfoLabel" />
            <TextView android:id="@+id/lengthText"
                style="@style/InfoLabel" />
            <TextView android:id="@+id/applicationText"
                style="@style/InfoLabel" />
            <TextView android:id="@+id/discoveryText"
                style="@style/InfoLabel" />
        </LinearLayout>
    </androidx.cardview.widget.CardView>
</ScrollView>
