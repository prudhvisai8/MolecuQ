<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:padding="16dp">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Top Bar -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <ImageView
                android:id="@+id/backButton"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_back"/>

            <TextView
                android:id="@+id/titleText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:text="Toxication Name"
                android:textSize="20sp"
                android:textColor="#000000"
                android:textStyle="bold" />
        </LinearLayout>

        <!-- Details -->
        <TextView android:id="@+id/formulaText" style="@style/InfoLabel" />
        <TextView android:id="@+id/symptomsText" style="@style/InfoLabel" />
        <TextView android:id="@+id/exposureSourcesText" style="@style/InfoLabel" />
        <TextView android:id="@+id/antidoteText" style="@style/InfoLabel" />
        <TextView android:id="@+id/severityText" style="@style/InfoLabel" />
        <TextView android:id="@+id/chemicalGroupText" style="@style/InfoLabel" />
        <TextView android:id="@+id/discoveryText" style="@style/InfoLabel" />

    </LinearLayout>
</ScrollView>
